import 'jest';

import { bool, string, StringShape } from '@punchcard/shape';

import { Query } from '../lib';
import { MyType } from './mock';

const underTest = Query.dsl(MyType);

it('should', () => {
  // tODO
  // expect(underTest.fields.id.between('lower', 'upper')).toEqual({
    
  // });
});